// Different terms for inputs
function compressBoxesTwice(boxes, boxes2)  { //dois inputs diferentes
    boxes.forEach(function(boxes) {
        console.log(boxes)
    })

    boxes2.forEach(function(boxes) {
        console.log(boxes)
    })
}


// O(a + b)